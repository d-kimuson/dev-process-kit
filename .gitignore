node_modules/
*.log
.DS_Store
.vite/
.direnv
.claude/trash/

# Build output, generated from `src/**` and never committed: `pnpm build` writes what
# `npm publish` ships, and `pnpm dev` rebuilds it for the samples.
dist/

# docs
docs/tmp
